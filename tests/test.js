'use strict'

var chai = require('chai');
var expect = chai.expect; // we are using the "expect" style of Chai

describe('test', function() {
    it('Just a small true test', function() {
        expect(true).to.be.true;
    });
});
